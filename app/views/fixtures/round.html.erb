<h1>Fixtures#round</h1>
<p>Find me in app/views/fixtures/round.html.erb</p>

<%= render 'fixtures_nav' %>

<%= render 'pages/db_schema', :locals => { :model => Fixture } %>

<% @fixtures.each do |fixture| %>
  <%= render 'fixture_card', :locals => { :fixture => fixture } %>
<% end %>
