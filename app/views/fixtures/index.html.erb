<h1>Fixtures#index</h1>
<p>Find me in app/views/fixtures/index.html.erb</p>

<table>

  <tr>
    <% Fixture.column_names.each do |col| %>
      <th><%= col %></th>
    <% end %>
  </tr>

  <% @fixtures.each do |fixture| %>
    <tr>
      <% Fixture.column_names.each do |col| %>
        <td><%= fixture[col] %></td>
      <% end %>
    </tr>
  <% end %>

</table>
