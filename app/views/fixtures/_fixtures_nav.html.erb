<div class="fixtures-nav">
  <% Fixture.pluck(:round).uniq.each do |round| %>
    <a
      href="<%= round_path(round) %>"
      class="<%= "selected" if round == params[:round] %>">
      <%= round %>
    </a>
  <% end %>
</div>
